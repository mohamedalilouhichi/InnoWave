export class Stage {
    id: number;
    title: string;
    description: string;
    // Vous pouvez ajouter d'autres propriétés selon les besoins, par exemple :

    // etc.
  
    constructor(id: number, title: string, description: string) {
      this.id = id;
      this.title = title;
      this.description = description;
     
    }
  }
  